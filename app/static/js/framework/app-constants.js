/**
 * App-wide constants
 *
 * Centralized constants for consistency across the application.
 * Note: State colors are now in state-colors.js (window.StateColors)
 */

window.AppConstants = {
    // Add app-wide constants here as needed
};
